@model BanSach.Models.Cart

@{
    ViewBag.Title = "ShowCart";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


@if (Model != null)
{
    <h2>Giỏ Hàng</h2>
    <a href="@Url.Action("ProductList", "Products")">Tiếp tục Mua hàng</a>

    @*<table class="table-bordered">
        <thead style="text-align:center; font-size:large;">
            <tr>
                <th>Tên Sản phẩm</th>
                <th>Hình</th>
                <th>Giá</th>
                <th>Số lượng</th>
                <th>Thành tiền</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Items)
            {
                decimal total = item._quantity * (decimal)item._product.Price.Value;
                <tr>
                    <td width="400">@Html.DisplayFor(model => item._product.NamePro)</td>
                    <td><img src="~/Content/images/@item._product.ImagePro" width="100" height="110" /></td>
                    <td width="100" align="right"> @Html.DisplayFor(model => item._product.Price)</td>
                    <td align="right">
                        @using (Html.BeginForm("Update_Cart_Quantity", "ShoppingCart"))
                        {
                            <input type="hidden" value="@item._product.ProductID" name="idPro" />
                            <input type="number" value="@item._quantity" name="carQuantity" style="text-align:right" />
                            <input type="submit" value="Cập nhật" />
                        }
                    </td>
                    <td width="100" align="right">@total.ToString("#,##0").Replace(',', '.')</td>
                    <td>
                        <a href="   RemoveCart/@item._product.ProductID" class="btn-danger">- Xóa -</a>
                    </td>
                </tr>
            }
        </tbody>
        <tfoot>
            <tr>
                <td colspan="3" align="center">
                    <br />
                    (Hãy đăng ký KH nếu chưa đăng ký)
                    <a href="@Url.Action("Create", "Customers")" class="btn btn-success">Đăng ký</a>
                </td>
                <td colspan="3" style="text-align:right; font-size:x-large; color: crimson;">
                    Tổng tiền : @Model.Total_money().ToString("#,##0").Replace(',', '.') Đồng
                </td>
            </tr>
            <tr>
                <td colspan="3">
                    <br />
                    <h4>-------------------THÔNG TIN ĐƠN HÀNG</h4>
                    @using (Html.BeginForm("CheckOut", "ShoppingCart"))
                    {
                        <table style="border-collapse:separate; border-spacing:5px;">
                            <tr>
                                <td></td>
                                <td><input type="hidden" name="CodeCustomer" value="@Session["IDCus"]" required /></td>
                            </tr>
                            <tr>
                                <td> Khách hàng : </td>
                                <td><input type="text" name="NameCustomer" value="@Session["NameCus"]" required /></td>
                            </tr>
                            <tr>
                                <td> Điên thoại : </td>
                                <td><input type="text" name="PhoneCustomer" value="@Session["PhoneCus"]" required /></td>
                            </tr>
                            <tr>
                                <td> Địa chỉ giao hàng : </td>
                                <td> <input type="text" name="AddressDeliverry" required /></td>
                            </tr>
                            <tr>
                                <td></td>
                                <td>
                                    <a href="@Url.Action("LoginCus", "Customers")" class="btn btn-primary">Đăng nhập</a>
                                    (Hãy đăng nhập trước)
                                    <input type="submit" value="Thanh toán" class="btn btn-danger" />
                                </td>
                            </tr>
                        </table>
                    }
                </td>
                <td colspan="3"></td>
            </tr>
        </tfoot>
    </table>*@

    <section class="fill-cart">
        <div class="container my-5">
            <div class="row justify-content-center">
                @foreach (var item in Model.Items)
                {
                    decimal total = item._quantity * (decimal)item._product.Price.Value;
                    <div class="card mb-xl-3 me-3" style="max-width: 540px;">
                        <div class="row g-0">
                            <div class="col-4 col-sm-4 col-md-4">
                                <img src="~/Content/images/@item._product.ImagePro"
                                        class="img-fluid rounded-start" alt="...">
                            </div>
                            <div class="col-8 col-sm-8 col-md-8">
                                <div class="card-body">
                                    <h5 class="card-title">@Html.DisplayFor(model => item._product.NamePro)</h5>
                                    <div class="card-text">
                                        <p style="color:red; float:left; padding-top:5px;">@Html.DisplayFor(model => item._product.Price)</p>
                                        @using (Html.BeginForm("Update_Cart_Quantity", "ShoppingCart"))
                                        {
                                            <span style="float:right">
                                                <input type="hidden" value="@item._product.ProductID" name="idPro" />
                                                <input type="number" class="rounded-1" value="@item._quantity" name="carQuantity" style="text-align:right;width:40px;"  />
                                                <input type="submit" class="btn btn-primary rounded-pill" value="Cập nhật" />
                                            </span>
                                        }
                                    </div>
                                    <p class="card-text" style="clear:both;">
                                        <span class="fs-5" style="font-weight:600;float:left;">Tổng : @total.ToString("#,##0").Replace(',', '.') ₫</span>
                                        
                                        <a href="RemoveCart/@item._product.ProductID" class="btn btn-danger" style="float:right;">Xóa</a>
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
            
            <div class="row justify-content-center">
                <div class="card">
                    <div class="card-header fw-bold fs-4 text-center" >
                        --------------------- ĐƠN HÀNG ---------------------
                    </div>
                    <div class="card-body">
                        <h5 class="card-title">
                            <span class="text-decoration-underline">Hãy đăng ký khách hàng nếu chưa đăng ký</span> ➩
                            <a href="@Url.Action("Create", "Customers")" class="btn btn-info">Đăng ký</a>
                        </h5>
                        <h5 class="card-title">
                            <span class="text-decoration-underline">Hãy đăng nhập nếu chưa đăng nhập</span> ➩
                            <a href="@Url.Action("LoginCus", "Customers")" class="btn btn-info">Đăng nhập</a>
                        </h5>
                        <h6 class="card-text fs-5 mb-3">Tổng tiền : @Model.Total_money().ToString("#,##0").Replace(',', '.') Đồng</h6>
                        @using (Html.BeginForm("CheckOut", "ShoppingCart"))
                        {
                            <input type="hidden" name="CodeCustomer" value="@Session["IDCus"]" required />
                            <p>
                                <text style="color:blue;padding-right:24px">Tên :</text>
                                <input type="text" name="NameCustomer" value="@Session["NameCus"]" required />
                            </p>
                            <p>
                                <text style="color:blue;padding-right:20px">SĐT :</text>
                                <input type="number" name="PhoneCustomer" value="@Session["PhoneCus"]" required />
                            </p>
                            <p>
                                <text style="color:blue;padding-right:px">Địa chỉ :</text>
                                <input type="text" name="AddressDeliverry" required />
                            </p>
                            <input type="submit" value="Thanh toán" class="btn btn-success fw-bold fs-5" style="width:150px;" />
                        }
                    </div>
                </div>
            </div>

        </div>
        </section>
}
else
{
    <section id="empty-cart">
        <div class="container my-5">
            <div class="row justify-content-center justify-content-md-center text-center">
                <div class="col-12">
                    <img class="img-fluid" src="~/Content/images/empty-cart.png" alt="empty-cart">
                    <div class="fw-bold fs-4">
                        Không tìm thấy sản phẩm nào trong giỏ hàng
                    </div>
                    <div>
                        <a href="@Url.Action("ProductList", "Products")">Quay lại mua hàng ...</a>
                    </div>
                </div>
            </div>
        </div>
    </section>
}